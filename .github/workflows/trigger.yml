on:
  issue_comment:
    types:
      - created
      - edited
      - deleted

jobs:
  like-pizza:
    runs-on: ubuntu-latest
    if: ${{ github.event.comment.body.includes(pizza)}}
    steps:
      - name: Like pizza
        uses: actions/github-script@v4
        with:
          script: |
            const {owner, repo} = context.issue
            github.reactions.createForIssueComment({
              owner,
              repo,
              comment_id: context.payload.comment.id,
              content: "+1",
            });

  trigger-quality-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # - name: GitHub Environment Variables Action
      #   uses: chillianosson/github-env-vars-action@v2

      - name: log values
        run: |
          echo "context is ${{ context.issue }}"
          echo "ref name is ${{ env.CI_ACTION_REF_NAME }} repo path is ${{ env.CI_REPOSITORY_SLUG }} head ref is ${{ env.CI_HEAD_REF }}"

      # - name: trigger quality-gate
      #   uses: "a string"
      #   with:
      #     e2e-branch: "Passed input data branch name from triggerrrrrrr"
